apiVersion: v1
kind: Service
metadata:
  name: predictive-maintenance-service
spec:
  type: LoadBalancer
  selector:
    app: predictive-maintenance-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000